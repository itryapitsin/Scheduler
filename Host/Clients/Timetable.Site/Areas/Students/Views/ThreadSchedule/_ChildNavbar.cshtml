<nav class="navbar navbar-default navbar-fixed-top navbar-child ng-scope" role="navigation" style="top:51px;">
    <div class="navbar-inner">
        <form class="navbar-form navbar-left" style="width:100%;">
            <div class="form-group col-sm-2 col-sm-offset-1">
                <select class="form-control" required="required" placeholder="Подразделение..." ng-change="branchChanged()" ng-model="currentBranchId" ng-options="branch.id as branch.name for branch in branches"></select>
            </div>
            <div class="form-group col-sm-2">
                <select class="form-control" required="required" placeholder="Форма обучения..." ng-change="studyFormChanged()" ng-model="currentStudyTypeId" ng-options="studyType.id as studyType.name for studyType in studyTypes"></select>
            </div>
            <div class="form-group col-sm-2">
                <select class="form-control" required="required" placeholder="Факультет..." ng-change="facultyChanged()" ng-model="currentFacultyId" ng-options="faculty.id as faculty.name for faculty in faculties"></select>
            </div>
            <div class="form-group col-sm-2">
                <select class="form-control" required="required" placeholder="Курс..." ng-change="courseChanged()" ng-model="currentCourseId" ng-options="course.id as course.name for course in courses"></select>
            </div>
            <div class="form-group col-sm-2">
                <select class="form-control" required="required" placeholder="Группа..." ng-change="groupChanged()" ng-model="currentGroupId" ng-options="group.id as (group.code + ' ' + group.specialityName) for group in groups"></select>
            </div>
            <div class="form-group col-sm-1">
                @*<button class="btn btn-default" data-original-title ng-click="search()"><i class="glyphicon glyphicon-search"></i></button>*@
                <button class="btn btn-default" ng-disabled="!canSaveReport()" data-original-title ng-click="getReportForGroup()"><i class="glyphicon glyphicon-download-alt"></i></button>
            </div>
        </form>
    </div>
</nav>