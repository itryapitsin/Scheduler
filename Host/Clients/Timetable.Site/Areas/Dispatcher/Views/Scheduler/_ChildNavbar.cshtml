<nav class="navbar navbar-default navbar-fixed-top navbar-child" role="navigation" style="top:51px;" >
    <div class="navbar-inner">
        <div class="container-fluid">
            <ul class="nav navbar-nav pull-left col-sm-2">
                <li>
                    <a><b>Сведения к расписанию для потока</b></a>
                </li>
            </ul>
            <form class="navbar-form navbar-left" style="padding: 0;">
                <button class="btn" ng-disabled="!selectedTicket" ng-click="edit(selectedTicket)"><i class="glyphicon glyphicon-pencil"></i></button>
                <button class="btn" ng-disabled="!selectedTicket" ng-click="unplan()"><i class="glyphicon glyphicon-trash"></i></button>
                <button class="btn" disabled="disabled"><i class="glyphicon glyphicon-calendar"></i></button>

                <div class="btn-group">
                     <button type="button" class="btn dropdown-toggle" data-toggle="dropdown">
                         <i class="glyphicon glyphicon-print"></i>
                         <span class="caret"></span>
                    </button>
                    <ul class="dropdown-menu" role="menu">
                        <li><a style="cursor:pointer" ng-style="(isFacultySelected()) && true || (thisStyle={opacity:0.5})" ng-click="getReportForFaculty()" >Отчет по факультету: {{currentFaculty.name}}</a></li>
                        <li><a style="cursor:pointer" ng-style="(isCourseSelected()) && true || (thisStyle={opacity:0.5})" ng-click="getReportForCourse()" >Отчет по курсу: {{currentCourse.name}}</a></li> 
                        <li><a style="cursor:pointer" ng-style="(isGroupsSelected()) && true || (thisStyle={opacity:0.5})" ng-click="getReportForGroups()" >Отчет по группам: {{currentGroupNames}} </a></li>
                    </ul>
                </div>

               
            </form>
            <ul class="nav navbar-nav pull-right">
                <li>
                    <a href="" ng-click="showThreadDialog()">
                        Параметры потока:
                        <b>{{currentBranch.name}}</b>,
                        <b>{{currentFaculty.name}}</b>,
                        <b>{{currentStudyTypeNames}}</b>
                        <b>{{currentCourse.name}}</b>,
                        <b>{{currentGroupNames}}</b>
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>