using System;
using NUnit;

using Microsoft.VisualStudio.TestTools.UnitTesting;
using NUnit.Framework;
using Timetable.Site.Controllers.Api;

namespace Tests
{
    [TestFixture]
    public class ScheduleApiControllerTests
    {
        [Test]
        public void CanGetScheduleByAllTest()
        {
            /*var scheduleController = new ScheduleController();

            int? lecturerId = null;
            int? auditoriumId = null;
            var groupIds = "";
            int? weekTypeId = null;
            var subGroup = "";
            var startTime = "";
            var endTime = "";

            scheduleController.GetScheduleByAll(lecturerId, auditoriumId, groupIds, weekTypeId, subGroup, startTime,
                endTime);*/
        }
    }
}
